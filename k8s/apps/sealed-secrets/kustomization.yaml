apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Nessun namespace globale: controller.yaml contiene risorse cluster-scoped
# (ClusterRole, ClusterRoleBinding, CRD) e namespaced (kube-system).
resources:
  - controller.yaml
