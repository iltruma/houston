apiVersion: v1
kind: Endpoints
metadata:
  name: houston
  namespace: infra-proxy
subsets:
  - addresses:
      - ip: 192.168.178.2
    ports:
      - port: 8006
---
apiVersion: v1
kind: Service
metadata:
  name: houston
  namespace: infra-proxy
  annotations:
    traefik.ingress.kubernetes.io/service.serversscheme: https
    traefik.ingress.kubernetes.io/service.serverstransport: infra-proxy-insecure-tls@kubernetescrd
spec:
  ports:
    - port: 8006
      targetPort: 8006
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: houston
  namespace: infra-proxy
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - houston.lab.paroparo.it
  rules:
    - host: houston.lab.paroparo.it
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: houston
                port:
                  number: 8006
